authSecret:
  create: false
  name: controller-manager  # references the secret we’ll create manually

githubWebhookServer:
  enabled: true

metrics:
  serviceMonitor:
    enabled: true

autoscaling:
  enabled: true
  minReplicas: 1
  maxReplicas: 5

syncPeriod: 1m
